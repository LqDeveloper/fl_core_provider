## 0.1.4

* **Refactor:** Split lifecycle & event-bus infrastructure into the standalone `fl_state_lifecycle` package; `fl_core_provider` re-exports it to keep the public API compatible.
* **Deps:** Add `fl_state_lifecycle ^0.1.1`.

## 0.1.3

* **Docs:** Update Flutter constraint to `>=3.0.0` in READMEs.
